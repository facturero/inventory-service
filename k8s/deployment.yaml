apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service-node
  labels:
    app: inventory-service-node
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service-node
  template:
    metadata:
      labels:
        app: inventory-service-node
    spec:
      initContainers:
        - name: inventory-migrate
          image: ${REGISTRY}/${IMAGE_NAME}:${IMAGE_TAG}
          securityContext:
            runAsUser: 0
          command:
            - sh
            - -c
            - "npx sequelize-cli db:migrate"
          env:
            - name: NODE_ENV
              value: "production"
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: host
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: port
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: name
      containers:
        - name: inventory-service
          image: ${REGISTRY}/${IMAGE_NAME}:${IMAGE_TAG}
          ports:
            - containerPort: 3013
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3013"
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: host
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: port
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: inventory-db
                  key: name
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: inventory-rabbitmq
                  key: url
            - name: CORS_ORIGIN
              value: "https://steep-geology-napping.ngrok-free.dev"
            - name: PLUGIN_CATALOG_SERVICE_URL
              value: "http://plugin-catalog-service:3011"
            - name: INTERNAL_USER_ID
              value: "00000000-0000-0000-0000-000000000000"
            - name: OTEL_SERVICE_NAME
              value: "inventory-service"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.observability.svc.cluster.local:4318"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            # Sin `limits` a proposito: un tope de CPU de 300m estrangulaba al servicio (throttle)
            # y las pruebas de stress-petitions medían el limite de Kubernetes, no la capacidad
            # del codigo, con el nodo casi ocioso. Se conservan los `requests` (reserva).
          livenessProbe:
            httpGet:
              path: /health
              port: 3013
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 3013
            initialDelaySeconds: 5
            periodSeconds: 10